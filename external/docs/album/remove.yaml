/albums/{id}:
  delete:
    tags:
      - Album
    summary: Remove a specific album (can do owner or admin of album)
    security: []
    parameters:
      - $ref: "#/definitions/uuid-param"
    responses:
      204:
        description: Album's successfully removed, all album's images album_id now is null
      401:
        $ref: '#/definitions/401'
      404:
        $ref: '#/definitions/404'
      403:
        $ref: '#/definitions/403'